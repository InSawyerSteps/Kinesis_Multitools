# Core MCP Server Framework & Pydantic v2 Stack
fastmcp==2.9.2
mcp==1.9.4
pydantic[email]==2.11.7
pydantic-settings==2.5.2
ollama==0.5.1
fastapi==0.115.9
uvicorn[standard]==0.30.6

# Security & Audit Tools (Pydantic v2 compatible)
safety==3.2.9
pip-audit==2.9.0
pip-licenses==5.0.0

# Core ML/embedding dependencies
faiss-cpu==1.11.0
sentence-transformers==4.1.0
jedi==0.19.2
chromadb==1.0.5
onnxruntime==1.21.1
numpy==2.3.1
tokenizers==0.21.1

###############################
# IMPORTANT: TORCH INSTALLATION
###############################
# DO NOT install torch via this requirements file.
# Install PyTorch separately so it matches BOTH your local CUDA and Python version.
# Follow the README section: "Install PyTorch (matching your CUDA)".
#
# Reference only (do not install blindly):
#   Example used on some machines: torch==2.7.1+cu128  (Windows, CUDA 12.8, Python 3.13)
#   Example from older desktop:    torch==2.5.1+cu121  (Windows, CUDA 12.1, Python 3.12)
#
# Choose a version from https://pytorch.org/get-started/previous-versions/ that supports your Python.
# Use the correct index URL:
#   CUDA 11.8 -> https://download.pytorch.org/whl/cu118
#   CUDA 12.6 -> https://download.pytorch.org/whl/cu126
#   CUDA 12.8 -> https://download.pytorch.org/whl/cu128
#   CPU only  -> https://download.pytorch.org/whl/cpu
#
# Python 3.13 note: libcst currently builds from source (no prebuilt wheel) and requires Rust.
# Either install Rust (rustup) or use Python 3.12 which has wheels for most libraries.

# Dependencies for /analyze and /edit multitools
pylint==3.2.2
mypy==1.16.1
bandit==1.8.5
vulture==2.11
radon==6.0.1
libcst==1.2.0

# Other common utilities
rich==14.0.0
typer==0.15.2
python-dotenv==1.1.0
requests==2.32.3
